---
distributions:
  ros1:
    distribution:
      - ros1.yaml
  ros2:
    distribution:
      - ros2.yaml
    # TODO(pbovbel) do we need cache?
    # distribution_cache: cache.yaml
type: index
version: 3
